
Zadanie A5

Co w powyższym kodzie robi instrukcja xor? zeruje
Po co w powyższych kodach jest instrukcja nop? wyrównuje dla skoku
Dlaczego kompilator wybrał do przechowywania zmiennej i rejestr ebx,
 a nie np. rejestr r10d, którego wartości nie musiałby zachowywać na stosie (zaoszczędziłby dwie instrukcje)?
; bo przy każdym wywołuwaniu funkcji w pętli musiałby push r10d a po pop r10d, nie zaoszczędziłoby
Dlaczego przed wywołaniem funkcji bar wartość rejestru ebx jest przepisywana do rejestru edi?
dla konwencji, że argumenty w edi dla funkcji
